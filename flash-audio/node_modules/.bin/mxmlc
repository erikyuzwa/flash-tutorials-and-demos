#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../flex-sdk/bin/mxmlc" "$@"
  ret=$?
else 
  node  "$basedir/../flex-sdk/bin/mxmlc" "$@"
  ret=$?
fi
exit $ret
